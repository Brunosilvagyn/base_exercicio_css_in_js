import { Form, Input, Button } from "./styles";

const FormVagas = () => {
  return (
    <Form>
      <Input placeholder="Digite a vaga" />
      <Button>Buscar</Button>
    </Form>
  );
};

export default FormVagas;
