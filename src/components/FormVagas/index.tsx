import * as S from './styles'

const FormVagas = () => {
  return (
    <S.Form>
      <S.Input type="search" placeholder="Front-end, Fullstack, UX, etc..." />
      <S.Button type="submit">Pesquisar</S.Button>
    </S.Form>
  )
}

export default FormVagas
